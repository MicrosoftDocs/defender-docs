name: Require Writer Review

on:
  workflow_call:

jobs:
  add-hold-off-comment:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Check if command author is a team member and add hold-off comment
        env:
          MSECD_READTEAM_TOKEN: ${{ secrets.MSECD_READTEAM_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number || github.event.issue.number }}
          REPO: ${{ github.repository }}
        run: |
          VERSION="1.2.0"
          echo "Hold-off Comment Workflow Version: $VERSION"
          
          if [ -z "$MSECD_READTEAM_TOKEN" ]; then
            echo "ERROR: MSECD_READTEAM_TOKEN is not set!"
            exit 1
          else
            echo "MSECD_READTEAM_TOKEN is configured."
          fi
          
          # Check if last PRMerger command is #sign-off
          echo "Checking last PRMerger command..."
          COMMENTS=$(curl -s -H "Authorization: token $MSECD_READTEAM_TOKEN" \
            "https://api.github.com/repos/$REPO/issues/$PR_NUMBER/comments?per_page=100")
          
          COMMAND_LINES=$(echo "$COMMENTS" | jq -r '.[] | .user.login as $u | .body | split("\n")[] | select(test("^#[A-Za-z-]+")) | "\($u)|\(.)"')
          LAST_COMMAND_LINE=$(echo "$COMMAND_LINES" | tail -n1)
          COMMAND_AUTHOR=${LAST_COMMAND_LINE%%|*}
          COMMAND_TEXT=${LAST_COMMAND_LINE#*|}
          LAST_COMMAND=$(echo "$COMMAND_TEXT" | tr '[:upper:]' '[:lower:]')
          
          echo "Last PRMerger command found: $LAST_COMMAND"
          echo "Last PRMerger command author: $COMMAND_AUTHOR"
          
          if [ -z "$LAST_COMMAND" ] || [ "$LAST_COMMAND" != "#sign-off" ]; then
            echo "Last command is not #sign-off. Skipping workflow."
            exit 0
          fi

          if [ -z "$COMMAND_AUTHOR" ]; then
            echo "ERROR: Unable to resolve command author."
            exit 1
          fi
          
          echo "Last command is #sign-off by the command author. Continuing workflow."
          
          # Use hardcoded team
          ORG="MicrosoftDocs"
          TEAM="msecd-org"
          echo "Resolving team: $ORG/$TEAM"
          
          # Fetch team members using MSECD_READTEAM_TOKEN
          RESPONSE=$(curl -s -w "\n%{http_code}" -H "Authorization: token $MSECD_READTEAM_TOKEN" \
            "https://api.github.com/orgs/$ORG/teams/$TEAM/members")
          
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          
          TEAM_MEMBERS=""
          
          if [ "$HTTP_CODE" = "200" ]; then
            MEMBERS=$(echo "$BODY" | jq -r '.[].login' 2>/dev/null || echo "")
            if [ -n "$MEMBERS" ]; then
              echo "Team members: $MEMBERS"
              TEAM_MEMBERS="$MEMBERS"
            else
              echo "No members found in team response."
            fi
          else
            echo "Failed to fetch team members. HTTP Status: $HTTP_CODE"
          fi

          # Remove duplicates and check if command author is a team member
          TEAM_MEMBERS=$(echo "$TEAM_MEMBERS" | tr ' ' '\n' | sort -u | tr '\n' ' ')
          echo "Team members: $TEAM_MEMBERS"

          IS_TEAM_MEMBER=false
          for user in $TEAM_MEMBERS; do
            if [ "$user" == "$COMMAND_AUTHOR" ]; then
              IS_TEAM_MEMBER=true
              break
            fi
          done

          if [ "$IS_TEAM_MEMBER" = false ]; then
            echo "Command author '$COMMAND_AUTHOR' is not a team member. Adding hold-off comment..."
            COMMENT_BODY="#hold-off\nThis PR requires a writer review before merging.\nThe review process typically takes 2-3 business days. For urgent requests, please contact the page's author or assigned team."
            
            curl -s -X POST \
              -H "Authorization: token $MSECD_READTEAM_TOKEN" \
              -H "Accept: application/vnd.github.v3+json" \
              "https://api.github.com/repos/$REPO/issues/$PR_NUMBER/comments" \
              -d "{\"body\":\"$COMMENT_BODY\"}"
            echo "Hold-off comment added successfully."
          else
            echo "Command author '$COMMAND_AUTHOR' is a team member. Skipping comment."
          fi
